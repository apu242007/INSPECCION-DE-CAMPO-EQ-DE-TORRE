node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example

# Planillas con PIN o datos de reparto en claro: nunca al repo.
*credenciales*
*reparto*.xlsx

# Salida temporal del script de SharePoint
sharepoint/*.log

# Artefactos de herramientas locales, no del proyecto
.claude-flow/
.claude/

# Carpetas desempaquetadas del generador de flujos (los .zip sí van al repo)
power-automate/paquetes/EQT-0*/

# URLs de disparadores: son publicas en el bundle igual, pero no van al repo
urls-flujos.local.txt

# Clave compartida entre los flujos y el secret. Publica en el bundle igual, pero no al repo.
clave.local.txt
